【2019.5.23】
迷迷糊糊的一天…

1. 
第一个问题是输入框{[{fileName}]}，本来不应该显示在这里的，可是却显示了，说明JS里有一个部分没有加载上。
这是一个Angular的符号，在JS里定义成这个样子。
//  Provider
myapp.config(function ($interpolateProvider) {
    $interpolateProvider.startSymbol('{[{');
    $interpolateProvider.endSymbol('}]}');
});


【2019.5.24】
1. AssertionError: .accepted_renderer not set on Response
把@api_view和@renderer_classes加在view函数前面
add the @api_view and the @renderer_classes decorator to your view.
https://stackoverflow.com/questions/55416471/how-to-resolve-assertionerror-accepted-renderer-not-set-on-response-in-django?answertab=oldest#tab-top

2. AssertionError: @api_view missing list of allowed HTTP methods
